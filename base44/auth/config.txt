// Auth configuration. Push with `npx base44 auth push`.
// Reconcile against `npx base44 auth pull` for your app before deploying.
{
  "passwordLogin": {
    "enabled": true
  },
  "socialLogin": {
    "google": { "enabled": true }
  }
}
